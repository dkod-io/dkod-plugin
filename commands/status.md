---
description: Show dkod session status — active sessions, modified files, connection info
disable-model-invocation: true
---

Call `dk_status` to show the current dkod session state. Display session ID, repo name, modified files, base commit, and other active sessions. If no sessions are active, tell the user to run `dk_connect` first.
